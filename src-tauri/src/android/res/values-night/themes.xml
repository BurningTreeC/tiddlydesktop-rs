<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (dark mode). -->
    <style name="Theme.tiddlydesktop_rs" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Dark fallback colors shown before TiddlyWiki palette is loaded -->
        <item name="android:statusBarColor">#333333</item>
        <item name="android:navigationBarColor">#333333</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
    </style>
</resources>
